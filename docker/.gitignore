# Secrets — NIEMALS committen!
# Alle .env-Varianten AUSSER .env.example ausschliessen — .env.save,
# .env.bak, .env.<datum> enthalten dieselben Tokens wie .env.
.env
.env.*
!.env.example

# Volumes-Backups (lokal)
*.bak
*.dump
*.sql
